# GitHub → Replit Auto-Import Configuration
# Este ficheiro garante que o Replit importa sempre as últimas alterações

# Repository URL
GITHUB_REPO=https://github.com/Ramos-bot/Mobitask-App.git

# Branch principal
MAIN_BRANCH=main

# Ficheiros críticos que devem sempre sincronizar
CRITICAL_FILES=(
  "package.json"
  "server.js" 
  ".replit"
  "Procfile"
  "firebaseConfig.js"
  "src/mobitaskBase/"
)

# Instruções para o Replit:
# 1. Import from GitHub: https://github.com/Ramos-bot/Mobitask-App.git
# 2. Auto-pull: ON (para importar alterações automaticamente)
# 3. Auto-deploy: ON (para fazer redeploy quando houver alterações)

# Comandos de deployment (já configurados no .replit):
# Install: npm ci
# Build: npm run build:web  
# Run: node server.js
# Health: /health
