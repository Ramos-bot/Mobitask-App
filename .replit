entrypoint = "index.js"
modules = ["nodejs-22"]
hidden = [".config", "package-lock.json", ".expo", "node_modules", ".git"]

run = "npm run replit:deploy"

[gitHubImport]
requiredFiles = [".replit", "replit.nix", "package.json", "package-lock.json"]

[nix]
channel = "stable-24_11"

[deployment]
run = ["sh", "-c", "npm install && npm run replit:deploy"]
deploymentTarget = "autoscale"
ignorePorts = false

[workflows]
runButton = "Deploy Production"

[[workflows.workflow]]
name = "Deploy Production"
author = "mobitask"
mode = "sequential"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm run replit:deploy"

[[ports]]
localPort = 5000
externalPort = 80

[env]
NODE_ENV = "production"
PORT = "5000"
