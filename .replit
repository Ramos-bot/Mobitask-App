entrypoint = "server.js"
modules = ["nodejs-22"]
hidden = [".config", "package-lock.json", ".expo", "node_modules", ".git", "dist"]

# Força o run local a usar o servidor Express
run = "node server.js"

[gitHubImport]
requiredFiles = [".replit", "replit.nix", "package.json", "package-lock.json"]

[nix]
channel = "stable-24_11"

# Define comandos do Deployment separadamente
[deployment]
install = ["bash", "-lc", "npm ci"]
build = ["bash", "-lc", "npm run build:web"]
run = ["bash", "-lc", "node server.js"]
deploymentTarget = "autoscale"
ignorePorts = false

[[ports]]
localPort = 5000
externalPort = 80

[env]
NODE_ENV = "production"
PORT = "5000"
